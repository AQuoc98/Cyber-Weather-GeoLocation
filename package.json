{
  "name": "weather-app",
  "version": "1.0.0",
  "description": "Write a CLI (command line interface) application to get weather",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "axios": "^0.19.0",
    "request": "^2.88.0",
    "yargs": "^13.3.0"
  }
}
